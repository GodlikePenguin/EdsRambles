---
const { date } = Astro.props;

const options = { year: "numeric", month: "long", day: "numeric" };
const formattedDate = new Intl.DateTimeFormat('en-GB', options).format(Date.parse(date))
---

<time datetime={date}>{formattedDate}</time>
